package:
    name: dynamiq_engine-dev
    version: "0.0.1"

source:
    path: ../../

requirements:
    build:
        - python
        - setuptools
        - numpy

    run:
        - python
        - numpy
        - scipy
        - pandas
        - openpathsampling-dev 0.0.0


test:
    requires:
        - nose
        - python-coveralls

    imports:
        - dynamiq_engine
