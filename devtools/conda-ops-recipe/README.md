This directory contains an alternate install procedure for OpenPathSampling
when the OPS conda install is down.

* `other_conda_build.sh`: Shell script to make this directory into an OPS
  conda-build directory.

